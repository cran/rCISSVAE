% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_load_models.R
\name{impute_with_cissvae}
\alias{impute_with_cissvae}
\title{Impute new data with a loaded Python CISS-VAE model}
\usage{
impute_with_cissvae(
  model,
  data,
  index_col = NULL,
  cols_ignore = NULL,
  clusters,
  imputable_matrix = NULL,
  binary_feature_mask = NULL,
  replacement_value = 0,
  batch_size = NULL,
  seed = 42
)
}
\arguments{
\item{model}{Python model object loaded via load_cissvae_model()}

\item{data}{R data.frame with missing values}

\item{index_col}{String name of index column to preserve (optional)}

\item{cols_ignore}{Character vector of column names to exclude from imputation scoring.}

\item{clusters}{Integer vector of cluster labels for rows of data}

\item{imputable_matrix}{Logical matrix indicating entries allowed to be imputed.}

\item{binary_feature_mask}{Logical vector marking which columns are binary.}

\item{replacement_value}{Numeric value used to replace missing entries before model input.}

\item{batch_size}{Batch size passed to Python DataLoader. If NULL, batch_size = nrow(data)}

\item{seed}{Base random seed for reproducible results}
}
\value{
Imputed R data.frame
}
\description{
Given a loaded model, an R data frame, and a vector of cluster labels,
this builds the Python ClusterDataset and DataLoader, runs inference,
and returns an imputed data frame in R.
}
\section{Tips}{

\itemize{
\item Use same ClusterDataset parameters as for initial model training.
\item Clusters must have same labels as clusters used for model training
\item 'binary_feature_mask' is required for correct imputation of binary columns.
}
}

\examples{
## Requires a working Python environment via reticulate
## Wrapped in try() and donttest to avoid CRAN check failures
\donttest{
try({
  # Activate your reticulate Python environment with ciss-vae installed
  reticulate::use_virtualenv("cissvae_environment", required = TRUE)

  # Load example data and clusters (replace with your own)
  data(df_missing)
  data(clusters)

  # Load a previously saved model
  model <- try(load_cissvae_model("model.pt", python_env = "cissvae_environment"))

  # Perform imputation on new data
  imputed_df <- try(
    impute_with_cissvae(
      model = model,
      data = df_missing,
      index_col = "index",
      cols_ignore = c("Age", "Salary"),
      clusters = clusters$clusters,
      imputable_matrix = NULL,
      binary_feature_mask = NULL,
      replacement_value = 0,
      batch_size = 4000L,
      seed = 42
    )
  )
})
}
}
